> 历史审计记录：下文“当前”与建议均指审计时点，部分实现已完成。当前进度见 [PROJECT_STATUS.md](PROJECT_STATUS.md)，不要据此重复迁移或执行清理。

# 当前完成情况

**下一阶段最适合先实现 `LibraryToolbar.vue`，完成“筛选＋排序＋Mock 网格更新”的小闭环。** 原型没有片库内搜索框，不应把搜索塞进 Toolbar。

本次依据当前本地仓库的文档、HTML、共享 CSS/JS 和 Vue 源码分析；未修改文件、未提交或推送。以下视觉判断来自源码对照及已有验收记录，本次没有重新运行浏览器视觉验收。

当前 [LibraryView.vue](frontend/src/views/LibraryView.vue) 已完成：

| 部分 | 当前实现与完成程度 |
|---|---|
| 页面入口 | 使用真实 `/library` 路由，业务页面留在 View 内 |
| 标题 | 已有“电影”，标题字号、字重、间距承接原型 |
| 内容宽度与网格 | 全宽、自适应列数、20px 间距，与原型桌面网格公式一致 |
| MovieCard | 2:3 海报、10px 圆角、片名、年份与类型，普通卡主要视觉已迁移 |
| 卡片交互 | Hover 遮罩、播放与更多按钮、菜单展开态；海报不缩放 |
| 图片失败 | 保留海报占位、片名及操作入口 |
| MovieContextMenu | 页面级单实例、168px 桌面浮层、锚点定位和防溢出 |
| 菜单操作 | 同一按钮切换、不同电影切换、外部点击关闭、滚动及 resize 关闭 |
| 个人状态 | 收藏与已看/未看可以修改页面 Mock 副本，刷新恢复 |
| Mock | 8 部电影及本地海报，与展示组件分离 |

`PROJECT_STATUS.md` 已记录 MovieCard、MovieContextMenu 的迁移与视觉验收完成。因此，下一阶段应复用它们。

但**播放、详情、媒体版本目前仅记录 intent**，不代表对应业务已完成；整个电影页也尚未达到完整原型效果。

# HTML 与 Vue 差距

主要依据：[电影页 HTML](html/personal-cinema-movie-library.html)、[共享样式](html/assets/css/personal-cinema-library-view.css)、[页面交互脚本](html/assets/js/personal-cinema-library-view.js)。

**1. 页面结构**

原型实际顺序为：

```text
电影标题
Toolbar：左侧筛选 / 右侧排序
电影与合集网格
首次加载 / 追加加载区域
筛选无结果 / 空片库状态
```

| 检查项 | 原型事实 | Vue 差距 |
|---|---|---|
| 页面标题 | 只有“电影” | 已实现 |
| 描述文字 | 电影页标题下没有描述 | 不需要补 |
| 统计信息 | 没有独立统计区、总数展示 | 不需要补 |
| Toolbar | 左筛选、右排序，滚动时吸顶 | 缺失 |
| 状态筛选 | 位于筛选浮层内部 | 缺失，不能另加 Tabs |
| 视图控制 | 没有网格/列表切换 | 不需要新增 |
| 搜索 | 侧栏入口打开独立搜索浮层 | Vue 侧栏目前仅显示未开放提示 |

**2. Toolbar 的真实内容**

筛选浮层桌面为两列，包含：

| 分组 | 原型选项 | 选择方式 |
|---|---|---|
| 类型 | 科幻、剧情、动作、悬疑、动画、喜剧、恐怖、纪录片 | 多选 |
| 年份 | 全部、2020–、2010–2019、2000–2009、1990–1999、更早 | 单选 |
| 状态 | 全部、未看、观看中、已看 | 单选 |
| 来源 | AList · 迅雷云盘、家庭 NAS、我的 Nextcloud | 多选 |
| 底部操作 | 清除筛选 | 立即重置 |

这些来源是当前用户的 **WebDAV 来源实例**，不是不同协议类型。

排序浮层包含：最近添加、片名 A–Z、上映年份、最近观看、时长。

Toolbar **没有搜索框、影片数量、收藏按钮或视图切换按钮**。“筛选 · N”中的 N 是选中条件数量，不是电影数量。

**3. Movie Grid**

| 项目 | 原型 | Vue |
|---|---|---|
| 网格宽度 | 内容区全宽，无额外窄容器 | 已一致 |
| 最小卡宽 | `clamp(150px, 9vw, 170px)` | 已一致 |
| 列数 | `auto-fill`，随窗口宽度变化 | 已一致 |
| 间距 | 20px | 已一致 |
| 海报比例 | 2:3 | 已一致 |
| 数据数量 | 15 部电影 | 8 部电影 |
| 默认展示 | 沙丘两部聚合为一个合集，共14个展示项 | 8张普通卡，无聚合 |
| 排序 | 默认最近添加 | 按 Mock 数组顺序，尚无排序逻辑 |
| 空状态 | 筛选无结果、空片库分别处理 | 均缺失 |
| 加载状态 | 首次12个骨架、追加6个骨架的演示能力 | 缺失 |

原型追加加载只是可调用的演示状态，**没有完整无限滚动或真实分页实现**，不应把它当成必须立即开发的分页系统。

另一个需要注意的差异是滚动容器：原型桌面在 `.app-main` 内滚动；Vue Shell 当前主要使用文档滚动。Toolbar 迁移时必须核对实际吸顶、浮层裁切和滚动表现，不能仅复制 `position: sticky` 就认为完成。

**4. 最新合集差距**

[合集 Mock](html/assets/js/personal-cinema-collection-mock.js) 已明确：

- 主电影页无筛选时，同一合集拥有至少两部电影才聚合。
- 有筛选条件时显示具体电影。
- 收藏、未看、已看、最近添加视图显示具体电影。
- 合集卡显示“沙丘系列 / 2 部电影”，左下为进入合集入口。
- 合集更多菜单只有“查看合集”，没有合集级收藏、播放或观看状态。

这是后续完整视觉迁移的待办，不能忽略；但应作为独立 Mock 切片，不把它扩展为正式 TMDB Collection 后端体系。

# 推荐组件拆分

建议先采用以下结构，不一次性创建所有候选组件：

```text
LibraryView.vue
├── 标题：留在页面
├── LibraryToolbar.vue：建议新增
│   ├── 筛选浮层：先内置
│   └── 排序浮层：先内置
├── 网格与空/加载状态：先留在页面
│   └── MovieCard.vue：复用
└── MovieContextMenu.vue：复用单实例
```

下面是**建议接口**，不是已确定的正式 API：

| 组件 | 职责 | Props | Emits | 是否独立 |
|---|---|---|---|---|
| `LibraryView` | 页面编排、Mock 数据、筛选与排序状态、结果派生、菜单协调 | 暂不需要 | 暂不需要 | 保持路由 View |
| `LibraryHeader` | 标题 | 若未来拆分可用 `title` | 无 | **暂不拆**，目前只有一个标题 |
| `LibraryToolbar` | 两个按钮、筛选/排序浮层、选中反馈和条件计数 | `filters`、`sort`、`genreOptions`、`sourceOptions` | `update:filters`、`update:sort`、`clear-filters` | **建议拆分** |
| `LibraryFilterPopover` | 四组筛选项 | 若拆分，接收筛选值与选项 | 更新、清除 | 先内置 Toolbar |
| `LibrarySortPopover` | 五种排序选项 | 若拆分，接收当前排序 | 更新排序 | 先内置 Toolbar |
| `MovieGrid` | 网格渲染及卡片事件转发 | 若拆分，接收展示项、菜单关联状态 | `detail`、`play`、`more` | 暂不拆；复用需求出现后再判断 |
| `MovieCard` | 普通电影卡展示与操作意图 | 保留 `movie`、`moreExpanded`、`menuId` | 保留 `detail`、`play`、`more` | 已独立，继续复用 |
| `MovieContextMenu` | 普通电影操作浮层 | 保留现有电影、锚点、状态参数 | 保留现有操作与关闭事件 | 已独立，继续复用 |
| `CollectionCard` | 合集海报、数量、进入合集 | 后续接收合集及拥有数量 | `detail`、`more` | 合集切片再拆 |
| `GlobalSearchDialog` | 全局搜索浮层与结果列表 | 后续接收 `open`、候选数据 | `close`、`select` | 搜索切片独立，挂在 Shell |

**应留在 LibraryView 的内容：**标题、当前筛选值、排序值、派生结果、空/加载状态选择、电影状态修改和单实例菜单协调。

Toolbar 自己只管理局部展开状态，例如 `null | 'filter' | 'sort'`，不读取 Mock、不承担电影查询。使用 `ref`、`computed` 足够。

# 数据结构变化建议

当前 [movie.ts](frontend/src/types/movie.ts) 足以支持普通卡和菜单，但不足以支持完整筛选、排序、搜索。

**筛选和排序阶段必须补足的信息：**

| 字段建议 | 用途与约束 |
|---|---|
| `genres: string[]` | 类型筛选；不能拿单个 `genreLabel` 代替完整类型 |
| `sourceIds: string[]` | 来源筛选；同一电影可有多个本人来源资源，仍只显示一张卡 |
| `addedAt` 或明确的 Mock 入库排序值 | 最近添加排序 |
| `lastPlayedAt` 或明确的 Mock 最近播放排序值 | 最近观看排序；与 `watchStatus` 分开 |
| `runtimeMinutes` | 时长排序，表示影片标准时长，不能混为资源实测时长 |

原型 `added`、`watched` 是数字排序样例，不是真实时间戳。可以先保留明确命名的 Mock 排序值，也可以使用固定测试时间；不要直接把 `15` 当日期。

另需独立的来源选项数据 `{ id, name }`，不需要地址、凭据或完整来源详情。

已有 `favorite`、`watchStatus`、`year` 不必重复新增。`genreLabel` 建议保留：例如原型“降临”的完整类型列表与卡片展示类型顺序并不相同，直接取 `genres[0]` 会改变卡片文案。

**后续切片再补：**

| 信息 | 加入时机 |
|---|---|
| `originalTitle`、导演/演员姓名 | 全局搜索迁移时 |
| `rating` | 搜索结果有评分时显示；普通卡和 Toolbar 不依赖 |
| Collection 的 ID、名称、海报、成员 Movie ID | 合集切片；独立建模 |
| `resolution`、字幕、音轨、文件实际 `duration` | 媒体版本或详情切片，属于资源信息 |
| 精确播放进度 | 有可靠播放反馈以后 |

全局搜索只需要可检索姓名，不必为了搜索引入整套演职员详情模型。

Mock 建议逐步补齐原型15部电影，以便同内容对照；空片库、无匹配结果、不同来源和排序结果应有可重复的验收数据。数据访问开始增长时，用一个薄 Service 包住 Mock，避免 View 长期直接依赖大型样例；不需要 Store 或复杂 Repository 抽象。

# 交互设计建议

**筛选**

- 点击筛选按钮切换浮层；点击外部关闭。
- 筛选与排序浮层互斥。
- 类型、来源使用 checkbox；年份、状态使用 radio。
- 勾选立即更新结果，无“应用”按钮。
- 同组类型/来源采用 OR，不同分组之间采用 AND。
- “未看”严格匹配 `unwatched`，不包含 `watching`。
- 清除筛选恢复默认条件，保留当前排序。
- 条件计数按选中项累计；年份、状态非“全部”时各计1。

原型筛选面板宽度上限390px、内部可滚动；迁移时保留两列组织及桌面鼠标操作。

**排序**

- 点击右侧按钮打开188px浮层。
- 当前项显示勾选；点击选项立即排序、更新按钮文字并关闭。
- 默认最近添加。
- 原型数字项降序：最新添加、最新观看、较新年份、较长时长优先。
- “片名 A–Z”实际使用 `localeCompare(..., 'zh-CN')`，不是拼音索引系统。
- 没有单独升降序切换按钮，不新增。

**搜索**

原型是：**侧栏搜索按钮 → 搜索浮层 → 输入即筛选 → 点击结果进入详情**。

- 输入框提示“搜索片名、导演或演员”。
- 无关键词展示前5条“最近内容”；源码并未单独按播放时间重排这5条。
- 非空关键词使用本地匹配；另有清空按钮、无结果提示和遮罩点击关闭。
- 搜索更新浮层结果，不直接过滤背后的电影网格。
- 不需要回车提交，也不新增搜索按钮。
- 原型自定义方向键、快捷键、手动焦点逻辑当前不迁移。
- 原型的 `ESC` 提示与当前不实现自定义 Escape 的约束需在搜索切片明确处理，避免保留无效操作提示。

保留原生按钮、输入框、自然 Tab 与基础语义；不添加手机 Action Sheet、Touch 分支或复杂键盘系统。

# 推荐开发顺序

| 步骤 | 内容 | 为什么这样安排 |
|---|---|---|
| **Step 1** | 迁移 `LibraryToolbar` 的按钮、两种浮层、展开/选中/清除反馈，并放回原型位置 | 先补齐页面最明显的缺失，确定真实视觉和交互边界 |
| **Step 2** | 补充最小类型和 Mock 字段，接通本地筛选、五种排序、条件计数；保留 `genre` URL 初始化语义 | 让 Toolbar 形成可使用闭环，不停留在静态控件 |
| **Step 3** | 补齐筛选无结果与空片库，联动观看状态修改；结果移除卡片时关闭相关菜单 | 覆盖正常操作会触发的结果变化 |
| **Step 4** | 补齐本地海报与对照数据，验证吸顶、滚动容器、浮层边界及窗口适配 | 数据和滚动行为会直接影响整页视觉，不能只验收单卡 |
| **Step 5** | 增加首次与追加加载的 Mock 状态，并收口薄 Service | 展示加载效果、保持数据访问边界，不提前实现真实分页 |
| **Step 6** | 独立完成合集卡、聚合与筛选展开规则 | 补齐最新原型，同时避免把 Collection 强塞进普通电影状态 |
| **Step 7** | 单独迁移 Shell 全局搜索，衔接后续详情入口 | 搜索跨页面，不属于 LibraryToolbar，适合独立切片 |

每一步都以 Windows Desktop Chromium + Mouse 验收，覆盖1366、1440、1600、1920px、窗口高度变化和 Windows scaling；对照时使用相同内容及状态。

API 当前仍为 [draft](docs/API.md)，筛选、排序、分页、合集尚未确认。原型 JS 中的接口注释不能直接当正式契约。当前只整理最小前端类型和 Mock Service 边界，无须编写真实 HTTP 请求。

# 下一步最适合实现的组件

**`LibraryToolbar.vue`。**

建议下一次实现任务限定为：

> 忠实迁移电影页桌面筛选与排序 Toolbar，接通最小 Mock 筛选/排序及筛选无结果状态，复用现有 MovieCard 和 MovieContextMenu，并完成同视口视觉对照。

这样能形成一个范围清晰、可验证的阶段成果；合集、全局搜索和详情入口继续保留为后续完整迁移任务。
